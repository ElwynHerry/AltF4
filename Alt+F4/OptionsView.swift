import SwiftUI

struct JouerView: View {
    var body: some View {
        VStack {
            Text("Page Jouer")
                .font(.largeTitle)
                .padding()
            
            Spacer()
        }
        .navigationTitle("Jouer")
    }
}
