import SwiftUI

struct OptionsView: View {
    var body: some View {
        VStack {
            Text("Prochainement")
                .font(.largeTitle)
                .foregroundColor(.gray)
                .padding()
            Spacer()
        }
        .navigationTitle("Options")
    }
}
