import SwiftUI

struct OptionsView: View {
    var body: some View {
        VStack {
            Text("Page Options")
                .font(.largeTitle)
                .padding()
            
            Spacer()
        }
        .navigationTitle("Options")
    }
}
